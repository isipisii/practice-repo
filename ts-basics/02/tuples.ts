// const user: (string | number)[] = ["hc", 1 ]

// tuples ex
// in the variable declration sets the types of the 3 element of an array, it should be ordered base on what ive put inside the tuples
// the maximum length of an array with tuple is based on how many types are put inside a tuple
// const user: [string, number, boolean] = [true, 1, "stringf"] // error since its not ordered

const user1: [string, number, boolean] = ["user", 1, false] //